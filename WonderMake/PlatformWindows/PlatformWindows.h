#pragma once

 // TODO: There are some things we should include or define before including Windows.h, Unreal Engine has an example of everything we'll need.

#include <Combaseapi.h>
#include <Windows.h>
